# FocusFlow - 效率追踪器 v1.1.1 使用说明

## v1.1.1 更新亮点（修复开机自启）

### 0. 修复开机自启不生效
- **原因**：Windows 11 的"启动应用"管理会将已写入注册表 Run 键的启动项标记为「已禁用」，导致即使勾选了开机自启，开机也不会自动启动
- **修复**：启用开机自启时同步写入 `StartupApproved\Run` 的「已启用」标记，并在读取时校验 exe 路径与启用状态，确保开机自启真正生效
- 若之前已勾选过开机自启，可在设置中先取消再重新勾选，或直接重新勾选即可生效

---

## v1.1 更新亮点（小憩与护眼 · 番茄工作法）

### 0. 小憩与护眼（自动弹窗休息）
- **自动检测高强度输入**：默认 30 分钟内按键超过 10000 次时，自动弹出**护眼提醒**
- **护眼提醒弹窗**：置顶显示"眼睛需要休息啦"，含 20 秒倒计时，可选择：
  - **「休息一下」**：关闭提醒；若番茄钟正在工作，自动切换到休息
  - **「继续工作」**：关闭提醒，继续记录
  - 倒计时结束未操作时自动关闭（视为继续工作）
- **防打扰机制**：提醒关闭后自动重置计数，且有冷却时间（默认 10 分钟）避免频繁弹出
- **可配置**（`config.ini` 的 `[rest]` 节）：`window_minutes` 检测窗口 / `key_threshold` 按键阈值 / `cooldown_minutes` 冷却时间 / `rest_seconds` 提醒倒计时 / `enabled` 总开关

### 1. 番茄工作法（计时器插件）
- 内置插件「番茄工作法」：在「插件管理」中双击或选中后点 [打开窗口]
- **工作/休息定时器**：默认工作 25 分钟、休息 5 分钟，后台秒级计时
- **按键统计联动**：工作时长内自动记录按键数，每个番茄钟的按键数据持久化保存
- **自动切换**：工作结束后自动进入休息（可勾选关闭）
- **今日汇总**：显示今日完成番茄钟数、总按键数、平均速度
- **历史记录**：最近 50 条番茄钟记录（类型/开始时间/实际时长/按键数）
- **时长自定义**：可调整工作时长与休息时长（1-120 分钟）
- 数据库保存在 `data/focusflow_pomodoro.db`

---

## v1.0 更新亮点（全新界面 · DeepSeek 风格）

### 0. 设置中心（新增）
- 头像（用户名）旁新增**「设置」按钮**，弹出设置对话框，集中管理：
  - **暗色模式**（亮/暗主题切换）
  - **开机自启**（开机后台静默运行）
  - **暂停记录**（隐私保护）
  - **显示悬浮窗**（开关悬浮窗）
  - **启动时直接进入托盘**：开启后，软件启动不显示主界面窗口，只在系统托盘运行（悬浮窗仍会显示）
- **移除底部"退出"按钮**：退出程序请右键托盘图标 →「退出程序」
- 底部操作栏已移除，所有操作（清理数据/压缩/清除今日按键/导出/隐藏到托盘）统一收进「设置」对话框

### 1. 全新"液态玻璃"界面
- **设计语言**：极简主义 + 液态玻璃（半透明白色玻璃卡片 + 柔和阴影 + 顶部高光）
- **配色方案**：科技蓝 #4D8CF7 主色、浅灰蓝渐变背景、深灰 #1A1A2E 主文字、中灰 #4A4A6A 辅助文字
- **背景**：柔和纵向渐变 + 右上/左下淡蓝光斑，营造流动感
- **顶部导航栏**：左侧产品 Logo + 品牌名，右侧头像 + 「设置」按钮
- **主卡片（核心数据）**：今日活跃 / 当前速度 / 周期总数 / 日均 / 最高单日 突出展示
- **无衬线字体**：统一 Segoe UI；界面不使用 emoji
- 支持亮色 / 暗色双主题，切换时玻璃卡片与背景自动重绘

### 2. 窗口尺寸记忆
- **手动拉长/减短/移动窗口后，重启软件保持上次的尺寸与位置**
- 尺寸与位置**单独保存在 `window_state.ini`**（与 config.ini 同目录），复制/替换 config.ini 不会重置窗口大小
- 首次运行仍默认 880x720 并居中显示；窗口最小尺寸放宽为 640x460，允许更大幅度调整

### 3. 悬浮窗风格适配与置顶增强
- 悬浮窗已按"液态玻璃"风格重绘：背景与文字背景完全一致，无四角色差；字体加粗清晰
- 跟随主界面主题（亮色/暗色）自动切换配色
- **启动自动显示**：软件启动时悬浮窗自动出现，无需手动点击；可在「设置」对话框勾选/取消"显示悬浮窗"（关闭后下次启动不再自动显示）
- **位置记忆**：拖动悬浮窗后位置自动保存（window_state.ini），重启软件后保持上次位置
- **置顶增强**：使用 Win32 置顶扩展样式 + 周期重申置顶，打开其他软件/游戏时悬浮窗不会被任务栏或其他窗口遮挡；且设为"工具窗口、不抢焦点"，点击悬浮窗不会打断正在使用的软件

### 4. 窗口居中显示
- **主界面居中**：首次运行时主窗口在屏幕水平居中、垂直略偏上显示，不再出现在左上角
- **弹窗居中**：记账本添加/修改/选择分类、定时任务、分类维护、首次使用提示等弹窗均居中显示

### 5. 界面精简与修复
- **移除插件管理的内嵌预览**：插件管理界面不再显示内嵌插件视图，双击插件或点击 [打开窗口] 按钮在独立弹窗中操作
- **修复 Edge 历史记录 30 天趋势报错**：原趋势图依赖 matplotlib（打包时已移除）导致报错；已改为 Tkinter Canvas 手绘，不依赖任何第三方库

---

### 6. 按键统计优化（长按自动重复过滤）
- **修复**：玩游戏/长按某键时，系统自动重复事件导致按键数每秒增加几十次，严重虚高
- **方案**：按住期间重复触发的事件不再计数，只有真正"按下-释放"一次才算一次；即使 release 事件丢失也有 15 秒兜底，不影响后续计数
- **可配置**：`config.ini` 的 `[listener] ignore_key_repeat`（默认 true）可开关，`key_repeat_stale_seconds`（默认 15）可调整兜底时长

### 7. 新增"清除今日按键"功能
- **背景**：本次优化只阻止新的长按虚高；之前已经产生的今日虚高数据需要手动清除
- **入口**：「设置」对话框中"清除今日按键"；或在"按键排行"表格中右键按键 → "清除该按键今日次数"
- **用法**：选中（或输入）某个按键名，确认后仅删除该键今天的记录，不影响历史和其他按键，同时重置今日计数与实时速度

### 8. 稳定性与流畅性优化
- **修复界面卡顿**：后台全量刷新时不再阻塞等待数据库写入（原先每次可能卡 0~2 秒）
- **修复 flush 残留 bug**：写入队列为空时请求刷新，事件不再残留导致后续"等待 2 秒超时"
- **清除操作后台执行**：删除按键数据在后台线程完成，界面不卡顿，完成后自动刷新

### 9. 记账本优化
- **收支记录按 ID 排序**：列表改为按记录 ID 降序排列，最新添加的记录在最上面
- **分页布局优化**：表格行数随窗口高度自适应，分页栏紧贴表格下方且始终可见，无需下拉整个界面；消除记录下方大片空白
- **恢复"距今多久"功能**：操作栏新增"距今多久"按钮，支持多选记录（Ctrl/Shift 多选），按"X年Y天"显示每条的距今时间（按记录名称显示）
- **修复月度汇总分类明细**：分类明细现按净值统计（收入为正、支出为负），同分类收入 500 + 支出 500 显示为 0.00，不再错误累加成 1000
- **分页固定每 10 条**：收支记录列表每页固定显示 10 条
- **日期范围筛选生效**：筛选栏的"从/到"日期现在真正参与查询

### 10. 移除鼠标统计（重大变更）
- **纯键盘统计**：移除全部鼠标数据统计功能（左键/右键/中键/滚轮），仅保留键盘按键统计
- **删除 mouse_counter 模块**：不再启动鼠标监听器，减少系统资源占用
- **数据库清理**：启动时自动删除旧的 mouse_stats 表，不影响 key_log 数据
- **界面更新**：「键鼠排行」视图更名为「按键排行」，列标题由「按键/鼠标」改为「按键」
- **悬浮窗优化**：显示纯键盘活跃数，不再合并鼠标点击数

### 11. 悬浮窗优化
- **尺寸缩减**：宽度由 120px 缩减至 80px（减少 1/3）
- **配色优化**：背景改为淡青色 (#E0F4F4)，字体改为黑色，更柔和护眼
- **健壮性增强**：刷新循环的 after() 调用增加异常保护，窗口销毁时不再报错

### 12. 数据库健壮性增强
- **连接超时**：SQLite 连接 timeout 由 10s 提升至 15s，busy_timeout 由 10s 提升至 15s
- **缓存优化**：新增 PRAGMA cache_size=8MB，提升查询性能
- **异常恢复**：DBConnection 的 close() 增加异常保护，提交/回滚失败不阻止连接释放
- **批量写入**：已有的重试 + 逐条兜底机制继续保留，保证数据不丢

### 13. 记账本（重大优化）
- **修复筛选 bug**：选择"全部"现在会查询全部记录，而不是把表格清空（修复了 radio button value=None 导致 SQL 变成 WHERE type='None' 的隐性 bug）
- **新增分类筛选**：可在筛选栏直接选择"分类"和"细分分类"进行筛选，下拉框选择即触发查询
- **新增关键词筛选**：支持按物品名/备注/分类/细分分类模糊搜索
- **新增修改功能**：选中记录后点击"✎ 修改选中"，或双击记录即可修改（不再只能删除后重新添加）
- **新增重置筛选**：一键清空所有筛选条件
- **分类层级修正**：梦幻西游现在正确地放在"游戏"分类下作为细分分类，不再出现在顶级分类
- **移除莫名其妙选项**：移除了原收入分类里突兀的"卖金"选项
- **分类数据持久化**：所有分类与细分分类现在存储在数据库中，可通过插件动态维护

### 14. 定时任务（重大优化）
- **新增间隔执行类型**：例如"每天 7:00-23:00 期间每 60 分钟执行一次"，格式 HH:MM-HH:MM|分钟数
- **新增任务编辑功能**：选中任务后点击"✎ 编辑"，或双击任务即可修改（不再只能删除后重新添加）
- **新增启用/禁用切换**：一键切换任务的启用状态，无需删除
- **新增调度校验**：保存前自动校验时间格式，避免保存错误配置
- **更友好的显示**：列表中的"执行时间"列现在显示完整描述（如"每 60 分钟 (07:00 ~ 23:00)"）

### 15. 插件系统（重大优化）
- **插件管理不再是空壳**：所有按钮均可用，包括加载/卸载/热重载/编辑/删除
- **新增热加载**：勾选"启用插件热加载"后，修改插件文件保存即自动重载，无需手动操作
- **新增删除功能**：选中插件后点击"删除"可同时卸载并删除 .py 文件
- **新增编辑功能**：点击"编辑"用系统默认编辑器打开插件 .py 文件
- **新增视图显示**：选中插件后，下方区域会显示插件提供的界面（如果插件实现了 get_view）
- **修复卸载问题**：旧版本卸载后插件仍留在字典中，新版本完全清理
- **修复加载冲突**：插件名变更后自动清理旧条目，不会出现"V1/V2 并存"的脏状态

### 16. 内置插件：记账本数据维护
- 专门用于维护记账本的分类与细分分类数据
- 添加/修改/删除顶级分类（可设置类型：支出/收入/双向）
- 添加/修改/删除细分分类
- 上移/下移调整分类排序
- 修改分类名时自动同步更新所有相关记账记录

## 功能介绍

FocusFlow 是一款个人效率与专注力分析工具，通过记录键盘活跃数据，帮助您了解自己的打字习惯和工作节奏。

### 核心功能
- 实时记录键盘活跃次数
- 统计每个按键的次数及占比排行
- 按键分组统计（字母/数字/功能键/修饰键/其他）
- 多时间维度查看：今日 / 7天 / 15天 / 30天 / 1年 / 总计
- 年度数据归档与查询
- 历史日期查询（指定某一天的统计）
- 实时打字速度（CPM，每分钟按键数）
- 趋势图表（近 7 天/30 天活跃趋势折线图）
- 暂停/恢复记录（隐私保护）
- 开机自启（开机后台静默运行）
- 关闭窗口自动最小化到系统托盘

### 小憩与护眼（v1.1）
- 检测连续高强度输入（默认 30 分钟 10000 键），自动弹出护眼提醒
- 20 秒倒计时提醒，可选「休息一下」或「继续工作」
- 提醒冷却机制，避免频繁打扰（`[rest]` 配置可调）

### 番茄工作法（v1.1）
- 内置插件「番茄工作法」：工作/休息定时器（默认 25 分钟工作 + 5 分钟休息）
- 每个番茄钟自动记录按键数，与统计联动
- 今日完成番茄钟数 / 总按键 / 平均速度汇总
- 最近 50 条番茄钟历史记录
- 工作结束后可自动进入休息

### 数据管理
- 年度归档：每年数据独立存储（data/focusflow_YYYY.db），自动控制文件大小
- 数据导出：CSV / HTML
- 清理旧数据：按保留天数删除
- 数据库压缩：VACUUM 回收空间
- 自动备份：退出时备份到 backup/ 目录（保留最近 5 个）
- 数据完全本地存储，不上传任何信息

### 记账本功能（v1.0）
- 记录收入和支出
- 按日期/月/年/全部/类型/分类/细分分类/关键词多条件查询
- 分页查询（每页 10 条）
- 计算距今多久（年+天，支持多选）
- 分类盈亏统计（如游戏投入/赚取）
- 双击或点击按钮修改记录
- 分类数据可通过"记账本数据维护"插件自由维护

### 定时任务功能（v1.0）
- 三种调度类型：
  • 每日定时（HH:MM）
  • 一次性（YYYY-MM-DD HH:MM）
  • 每日间隔执行（HH:MM-HH:MM|分钟数）
- 启用/禁用切换
- 选中后双击或点击"编辑"修改任务
- 后台线程检查，到点自动执行

### 插件系统功能（v1.0）
- 动态加载/卸载/重载/删除/编辑插件
- 热加载：自动检测文件变更并重载
- 选中插件后显示其界面
- 内置"记账本数据维护"插件
- 内置"番茄工作法"插件（v1.1）

### 体验增强
- 全局热键 Ctrl+Shift+F 显示/隐藏主窗口
- 悬浮窗（画中画）：置顶小窗口实时显示今日活跃和速度
- 系统托盘：悬停显示"今日活跃 X 次 | 速度 XX 键/分"，暂停时图标变灰
- 亮色/暗色主题切换
- 现代化界面设计（Windows 11 风格）
- 首次启动提示可关闭（"不再显示"）

### 命令行接口（CLI）
```
FocusFlow.exe --stats 7            # 输出最近 7 天统计摘要
FocusFlow.exe --stats today        # 今日统计
FocusFlow.exe --stats all          # 总计
FocusFlow.exe --stats-year 2025    # 指定年度统计
FocusFlow.exe --list-years         # 列出所有有数据的年份
FocusFlow.exe --export csv         # 导出 CSV
FocusFlow.exe --export html        # 导出 HTML
FocusFlow.exe --vacuum             # 压缩数据库
FocusFlow.exe --cleanup 30         # 清理 30 天前的数据
FocusFlow.exe --reset              # 清空所有记录（需二次确认）
```

## 打包生成 exe（首次使用）

### 一键打包
1. 确保已安装 Python 3.8 或更高版本
   - 下载地址：https://www.python.org/downloads/
   - 安装时务必勾选 "Add Python to PATH"
2. 双击运行 `build.bat`
3. 等待打包完成，exe 文件在 `dist` 文件夹中

### 打包说明
- `build.bat` 优先使用 `FocusFlow.spec` 配置文件打包（推荐）
- 若 spec 失败，自动回退到命令行模式（含所有 hidden-import）
- 打包前会自动清理 build/ 和 dist/ 目录

### 打包后报错排查
如果 exe 运行时弹出 "No module named 'xxx'" 错误：
1. 确认所有 .py 文件都在同一目录
2. 重新运行 build.bat（会自动清理重建）
3. 若仍失败，手动执行：`pyinstaller --noconfirm FocusFlow.spec`
4. 查看控制台输出的错误信息

## 在新电脑上部署（免安装）

**直接用软件包即可运行，无需安装任何环境：**
- `FocusFlow.exe` 是单文件自包含程序（PyInstaller 打包），已内置 Python 运行库、Tkinter、以及 pynput / pystray / Pillow 等全部依赖
- 新电脑上**不需要安装 Python**，也不需要 pip 安装任何库
- 使用方法：把 zip 解压到任意文件夹（建议放在普通用户文件夹，如 `D:\FocusFlow`），双击 `FocusFlow.exe` 即可

**运行要求：**
- Windows 10 / 11（64 位）
- 所在文件夹需有写入权限（程序会在该文件夹生成 config.ini、data、logs、plugins、window_state.ini）
- 首次运行时部分杀毒软件可能对"键盘监听"报提示，选择"允许/添加信任"即可（数据完全本地，不上传任何信息）
- 数据都保存在 exe 所在目录，更换电脑时把整个文件夹（含 data）一起拷贝即可迁移历史数据

## 使用方法

### 启动程序
双击 `FocusFlow.exe` 运行，程序启动后自动开始记录。
首次启动会弹出使用提示（可勾选"不再显示"）。

### 查看统计
- 顶部显示今日活跃次数和实时速度
- 点击统计周期按钮切换（今日/7天/15天/30天/1年/总计）
- 年度下拉框可选择查看历史年度数据
- 在日期输入框中输入 YYYY-MM-DD 格式日期，点击"查询"可查看指定日期
- 点击"今天"按钮回到今日统计
- 表格显示按键排行（排名、按键、次数、占比）
- 切换到"分组统计"标签页可查看按键分组汇总
- 切换到"趋势图"标签页可查看近 7/30 天活跃趋势

### 记账本使用（v1.0）
- 在视图下拉框中选择"记账本"
- 点击"➕ 添加记录"添加收入/支出
- 在筛选栏选择类型/分类/细分分类，或输入关键词进行筛选
- 选中一条记录后点击"✎ 修改选中"，或直接双击记录即可修改
- 选中后点击"删除选中"可批量删除（支持多选）
- 点击"距今多久"查看选中记录距今多久
- 点击"分类盈亏"查看各分类的盈亏统计
- 点击"查询"进行按日期/月份/年份/关键词查询

### 定时任务使用（v1.0）
- 在视图下拉框中选择"定时任务"
- 点击"➕ 添加任务"添加任务
- 选择调度类型：
  • 每日定时：每天指定时间执行（如 08:30）
  • 一次性：指定日期时间执行一次（如 2026-12-31 23:59）
  • 每日间隔执行：每天指定时间段内每隔 N 分钟执行一次
    （如 07:00-23:00|60 表示每天 7~23 点每 60 分钟一次）
- 选中任务后双击或点击"✎ 编辑"修改
- 点击"启用/禁用"切换任务状态
- 点击"删除选中"删除任务

### 插件管理使用（v1.0）
- 在视图下拉框中选择"插件管理"
- 点击"新建插件"创建插件模板（会生成 .py 文件，需编辑后生效）
- 选中插件后可执行：
  • 加载：加载未加载的插件
  • 卸载：卸载已加载的插件
  • 热重载：手动重载插件（用于修改后立即生效）
  • 编辑：用系统默认编辑器打开插件 .py 文件
  • 删除：卸载并删除插件 .py 文件（不可恢复）
- 勾选"启用插件热加载"后，修改/新增插件文件会自动重载
- 选中已加载的插件后，下方会显示插件提供的界面

### 记账本数据维护插件（v1.0）
- 在"插件管理"视图中加载"记账本数据维护"插件
- 选中后下方显示该插件的界面
- 左栏操作分类：
  • 类型筛选：全部/支出/收入/双向
  • 添加/修改/删除分类
  • 上移/下移调整排序
- 右栏操作细分分类：
  • 先在左栏选中一个分类
  • 然后 添加/修改/删除 该分类下的细分分类
- 修改分类名会自动同步更新所有相关记账记录
- 删除分类不会影响已存在的记账记录（保留 category 字符串）

### 暂停记录
- 打开「设置」对话框（头像旁"设置"按钮），勾选/取消"暂停记录"；或右键托盘菜单选择"暂停记录"
- 暂停时托盘图标变灰，底部显示 [已暂停] 标识
- 暂停期间不会记录任何按键（隐私保护）

### 最小化到托盘
- 点击窗口右上角 X 关闭按钮 = 最小化到托盘，程序后台继续运行
- 也可以打开「设置」→"隐藏到托盘"
- 双击托盘图标 或 右键→"显示统计面板" 可恢复窗口
- 全局热键 Ctrl+Shift+F 也可显示/隐藏窗口

### 开机自启
- 打开「设置」对话框（头像旁"设置"按钮），勾选"开机自启"
- 开机后程序自动后台启动，不弹窗口，仅在托盘显示图标
- 再次取消勾选即可关闭

### 启动时直接进入托盘
- 打开「设置」对话框，勾选"启动时直接进入托盘（不显示主窗口）"
- 开启后，软件启动不显示主界面窗口，只在系统托盘后台运行（悬浮窗仍会自动显示）

### 完全退出程序
- 右键点击系统托盘图标 → 选择"退出程序"
- 退出时会自动：刷新待写入数据 → 备份数据库 → 释放资源

## 文件结构

程序运行后会在 exe 同目录生成以下文件：

```
FocusFlow.exe                  主程序
focusflow.ico                  应用图标
config.ini                     配置文件（可手动编辑）
plugins/                       插件目录（用户可在此放 .py 插件）
  accounting_data_maintenance.py  内置：记账本数据维护插件
data/
  focusflow_accounting.db      记账数据库（含分类表）
  focusflow_scheduler.db       定时任务数据库
  focusflow_2026.db            2026 年度键盘统计数据库
  focusflow_2025.db            2025 年度键盘统计数据库（归档）
  focusflow_2024.db            2024 年度键盘统计数据库（归档）
logs/
  focusflow.log                当前日志
  focusflow.log.1              轮转日志
backup/
  focusflow_2026_YYYYMMDD_HHMMSS.db   自动备份
```

## 插件开发指南

### 最简插件模板
```python
# -*- coding: utf-8 -*-
PLUGIN_NAME = "我的插件"
PLUGIN_DESC = "插件描述"
PLUGIN_VERSION = "1.0"
PLUGIN_AUTHOR = "作者"

def init():
    """插件初始化（可选）"""
    pass

def get_view(parent):
    """返回插件界面（可选）"""
    import tkinter as tk
    from tkinter import ttk
    frame = ttk.Frame(parent)
    ttk.Label(frame, text="Hello from 插件").pack(pady=20)
    return frame

def cleanup():
    """插件清理（可选）"""
    pass
```

### 插件开发要点
- 文件放在 `plugins/` 目录下，文件名建议用英文（如 `my_plugin.py`）
- 必须定义 `PLUGIN_NAME`、`PLUGIN_DESC`、`PLUGIN_VERSION`
- 可选实现 `init()`、`get_view(parent)`、`cleanup()`
- `get_view(parent)` 接收父容器，返回 ttk.Frame
- 开启热加载后，修改文件保存即自动重载
- 出错不会影响其他插件和主程序

## 配置文件说明（config.ini）

首次运行自动生成，可用文本编辑器修改：

```ini
[database]
batch_size = 100              ; 批量写入阈值
flush_interval = 30           ; 定时刷新间隔（秒）
backup_on_exit = true         ; 退出时自动备份
max_backups = 5               ; 最大备份数
auto_vacuum_days = 7          ; 每 N 天自动 VACUUM（0=禁用）
yearly_archive = true         ; 启用年度归档

[stats]
cpm_window = 60               ; CPM 计算窗口（秒）
today_count_cache_ttl = 10    ; 今日计数缓存有效期（秒）

[listener]
ignore_modifier_keys = false  ; 是否忽略修饰键
ignore_function_keys = false  ; 是否忽略 F1-F12

[gui]
refresh_interval = 2          ; 增量刷新间隔（秒）
full_refresh_interval = 30    ; 全量刷新间隔（秒）
show_first_run_tip = true     ; 是否显示首次提示
theme = light                 ; light / dark

[hotkey]
toggle_window = ctrl+shift+f  ; 显示/隐藏主窗口热键

[floating]
enabled = false               ; 启动时是否显示悬浮窗
opacity = 0.85                ; 透明度 0.1-1.0
```

## 注意事项

1. 本工具仅用于统计个人键盘使用情况，请在自己的电脑上使用
2. 部分安全软件可能报毒（键盘 Hook 机制），添加信任即可
3. 如需卸载：先取消开机自启 → 右键托盘退出程序 → 删除文件
4. 数据库损坏时，可从 backup/ 目录恢复
5. 日志文件位于 logs/ 目录，排查问题时可参考
6. 年度数据库位于 data/ 目录，可按需备份或迁移
7. 首次启动会自动迁移旧版分类数据到新表，原有记账记录不受影响
