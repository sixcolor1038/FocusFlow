{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "FocusFlow 默认能力：核心权限 + 悬浮窗拖动所需写入权限",
  "windows": ["main", "floating"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position"
  ]
}
